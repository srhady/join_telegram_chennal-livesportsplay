name: Auto Update Playlist

on:
  push:
    branches:
      - main
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  build-and-update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Setup Chrome
        uses: browser-actions/setup-chrome@v1

      # --- মূল পরিবর্তন এখানে ---
      # ধাপের নামটি ঠিক করা হয়েছে এবং সব লাইব্রেরি একসাথে ইনস্টল করা হচ্ছে
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install selenium beautifulsoup4

      - name: Run scraper from secret
        env:
          PYTHON_SCRIPT: ${{ secrets.SCRAPER_SCRIPT }}
        run: |
          echo "$PYTHON_SCRIPT" > run_scraper.py
          python run_scraper.py

      - name: Commit and push if there are changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add playlist.m3u
          if ! git diff --staged --quiet; then
            git commit -m "CHORE: Auto-update playlist (via Selenium)"
            git push
          else
            echo "No changes to commit."
          fi

